# Python
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
dist/
build/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Secrets — NEVER commit these
.env

# Runtime data — bind-mounted volume; backed up separately
data/

# Devcontainer's local runtime data (if streamlit is run inside the container)
.devcontainer-data/

# IDE
.vscode/
.idea/
*.swp

# Graphify knowledge graph — regenerated locally by post-commit/post-checkout git hooks.
# Run `graphify update .` manually after a fresh clone.
graphify-out/

